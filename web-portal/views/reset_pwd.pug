//-
    /***************************************************************************
     * 
     *   reset_pwd.pug:  admin page for online.interlisp.org web portal. 
     * 
     *   2021-11-22 Frank Halasz
     * 
     * 
     *   Copyright: 2021-2022 by Interlisp.org 
     * 
     *
     **************************************************************************/

extends layout

block variables
    - var title = "Interlisp Online"

block headeradds



block content
    h2.text-center Interlisp Online
    
    form(action="/admin/reset_pwd", method="post")
        .title
            h3 Reset Password
        .field
            label Email:
            input(type="text" name="username")
            br
        .field
            label Password:
            input(type="password" name="password" required)
            br
        .field
            input.submit-btn(type="submit" value="Submit" required)
    label#error-message

    script.
        const urlParams = new URLSearchParams(window.location.search);
        const info = urlParams.get('info');
    
        if(info) {
          const errorMessage = document.getElementById("error-message");
          errorMessage.innerText = info;
          errorMessage.style.display = "block";
        }

